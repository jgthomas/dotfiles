[user]
	name = James Thomas
	email = codeactor@protonmail.com
[core]
	editor = vim
	excludesfile = ~/.gitignore
[color]
	ui = true
[credential]
	helper = store
[alias]
    lg = "log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit"
    st = !git branch -avv && echo "" && git status -s
